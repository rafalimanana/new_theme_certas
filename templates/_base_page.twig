{% block html_head_container %}
{% include 'html-header-page.twig' %}
{% block head %}
{% endblock %}
</head>
{% endblock %}

<body class="{{body_class}}" data-template="_base_page.twig">
    {# <a class="skip-link screen-reader-text" href="#content">{{ _e( 'Skip to content') }}</a> #}
    <header class="header">
        {% include "_menu.twig" %}
        {% block header %}
        {% endblock %}
    </header>

    <section role="main" class="content-wrapper _content_pg" {# style="background-image: url('{{ site.theme.link }}/images/_bg/_bg_img.png}}')" #}>
        {% if title %}<h1>{{title}}</h1>{% endif %}
        
        {% block content %}
            Sorry, no content
        {% endblock %}

        {% if sidebar %}
            <aside class="layout-sidebar">
                {{sidebar}}
            </aside>
        {% endif %}
    </section>

    {% block footer %}

    {% include 'footer.twig' %}

    {{ function('wp_footer') }}
    {% endblock %}
    <script type="text/javascript">
        $('.close, .close-end').on('click', function(){
                $('.popup').hide();
        });
    </script>
</body>

</html>